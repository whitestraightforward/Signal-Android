package dev.chat.fork.messenger.groups.ui.invitesandrequests.joining;

enum JoinGroupError {
  BUSY,
  GROUP_LINK_NOT_ACTIVE,
  BANNED,
  FAILED,
  LIMIT_REACHED,
  NETWORK_ERROR,
  GROUP_TERMINATED,
}
